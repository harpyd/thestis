---
author: Djerys
title: invalid fixture specification
description: simple invalid HTTP method fixture specification

stories:
  test:
    description: test
    asA: test
    inOrderTo: test
    wantTo: test
    scenarios:
      test:
        description: test
        theses:
          test:
            when: test
            http:
              request:
                method: GED
                url: https://something.net/test
              response:
                allowedCodes:
                  - 201
                allowedContentType: application/json

          assert:
            then: test
            after:
              - test
            assertion:
              with: JSONPATH
              assert:
                'test.response.body.test': test